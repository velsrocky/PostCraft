[package]
name = "postcraft_media"
version = "0.1.0"
description = "FFmpeg discovery, capability probing, and supervised render jobs for PostCraft"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
